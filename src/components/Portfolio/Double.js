import React from 'react';

// A wrapper component that fishes out a ref of a DOM element in interest
// in a child component, duplicates it, hides the original element and
// animates the doubler.
//
// The child component must be a class.
//
// Can it do reverse? Let me make it work forward first.
const Double = props => {
  return <p>1</p>;
};

export default Double;
